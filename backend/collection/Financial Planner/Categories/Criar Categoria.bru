meta {
  name: Criar Categoria
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/categories
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Relógios"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
